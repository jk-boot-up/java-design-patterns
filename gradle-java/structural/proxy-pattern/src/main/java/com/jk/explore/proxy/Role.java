package com.jk.explore.proxy;

/**
 * Who is looking at the catalog. A shopper sees published imagery; a catalog
 * admin also sees assets that have not been released yet.
 */
public enum Role {
    SHOPPER,
    CATALOG_ADMIN
}
