spring:
  application:
    name: mobile-bff

server:
  port: 8080

# Where the shop is. The only thing in this backend's configuration that is not
# this team's own business, which is roughly the right proportion.
shop:
  base-url: http://localhost:8082

management:
  endpoints:
    web:
      exposure:
        include: health
