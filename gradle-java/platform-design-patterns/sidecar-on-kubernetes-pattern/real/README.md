# Tier 2 — the same two containers, as a Pod on a real cluster

Tier 1 makes the argument with a plain-Java model of a Pod. This directory runs
the real thing: a `kind` cluster, the **same two service images** as
[`sidecar-pattern`](../../sidecar-pattern/real), and the **same nginx template**, mounted
as a ConfigMap rather than copied. Nothing here is a fork of the Docker Compose version, so the comparison is exact.

**Tier 2 is optional.** Skipping it loses none of the pattern. [`../docs/dependencies.md`](../docs/dependencies.md)
says what Kubernetes is, what it costs, and how to skip it. This page is the transcript, which is real output.

```bash
./demo.sh          # creates the cluster, runs six acts, deletes the cluster
```

One command starts everything and the same command stops it: `demo.sh` deletes the cluster when it finishes, or
when it fails. To delete one by hand: `kind delete cluster --name sidecar-demo`.

Requires Docker (running), `kind` **0.33.0** (the script refuses any other), `kubectl`, a JDK 21, and a network
the first time. `kind` pulls a node image of about a gigabyte. A cold run took about four and a half minutes; a run with the image
already downloaded took about one.

## What is running

| Object | What it is |
| --- | --- |
| `gateway` Deployment and Service | The payment provider from `sidecar-pattern`, unchanged. HTTPS only, TLS 1.3 only. |
| `checkout` Deployment | **One Pod, two containers.** The service, and the proxy beside it as a *native sidecar*. |
| `refunds` Deployment | One container, exactly as its team wrote it. Act 6 gives it a second, from outside. |
| `sidecar-config` ConfigMap | `sidecar-pattern`'s nginx template, the one file every proxy reads. |
| `gateway-cert` Secret | The provider's certificate, generated by `demo.sh`. |

### The two lists in `k8s/checkout.yaml`

`containers` holds the service. `initContainers` holds the proxy, with one extra line, `restartPolicy: Always`. That
line makes it a **native sidecar**: it is started before the service, must be running before the service starts,
and keeps running beside it. What is missing matters as much: there is no `network_mode`, no `depends_on`, and no
network configured anywhere.

## The transcript

Captured from a real run. Never edit it by hand.

```
Building the two images from sidecar-pattern's sources

Creating the cluster (one node, in one Docker container)
  node image: kindest/node:v1.37.0
WARNING: version difference between client (1.27) and server (1.37) exceeds the supported minor version skew of +/-1
  Kubernetes: v1.37.0
========================================================================

Act 1 — kubectl get pods, and 2/2
========================================================================
  checkout-58fb4cf797-65clx  READY 2/2  Running  restarts 0
  gateway-5bf9748f85-cdx6k  READY 1/1  Running  restarts 0
  refunds-56b78d8c49-dtk7g  READY 1/1  Running  restarts 0
  the sidecar is a native sidecar (an init container with restartPolicy: Always):
    sidecar started 2026-09-19T09:53:56Z, checkout started 2026-09-19T09:53:58Z
    the sidecar started first: yes

  checkout reads 2/2: one logical service, two containers. refunds reads 1/1,
  because it is one container, and the next act gives it a second.
========================================================================

Act 2 — one network, by definition
========================================================================
  from inside the checkout container, on the loopback address, with no network configured anywhere:
    checkout sidecar up
  the address of the Pod, and of each container in it:
    pod IP: 10.244.0.6
    containers: sidecar checkout 

  The checkout container reached a port that belongs to a different container,
  and the manifest contains no network settings at all. That is a Pod.
========================================================================

Act 3 — a payment through the sidecar
========================================================================
  the provider is told to decline the first two attempts at each payment. checkout pays once:
    {
        "outcome": "paid",
        "reference": "order-1",
        "service": "checkout",
        "note": "attempt 3",
        "receipt": "pay_order-1",
        "amountPence": 4200
    }
  what the provider saw:
    {
        "allowance": 12,
        "total": 3,
        "refused": 0,
        "byService": {
            "checkout": 3
        },
        "transport": "TLSv1.3 (the caller presented a certificate)"
    }
  the proxy's own log, read from inside its container:
    checkout  10.96.185.238:9443, 10.96.185.238:9443, 10.96.185.238:9443  status=503, 503, 200  took=0.023, 0.008, 0.007  total=0.038
========================================================================

Act 4 — kill the proxy: only the proxy restarts
========================================================================
  before: restart counts (sidecar, checkout): 0 0
./demo.sh: line 145: 40284 Terminated: 15          k port-forward "pod/$CHECKOUT_POD" 18080:8080 > /dev/null 2>&1
./demo.sh: line 145: 40285 Terminated: 15          k port-forward "deployment/gateway" 19443:9443 > /dev/null 2>&1
  after:  restart counts (sidecar, checkout): 1 0
  checkout's start time is unchanged: 2026-09-19T09:53:58Z
  checkout-58fb4cf797-65clx  READY 2/2  Running

  The kubelet restarted the container that died, and only that one. A crash
  does not take a neighbour with it. What the Pod shares is scheduling and
  deletion, which is the next act.
========================================================================

Act 5 — delete the Pod: everything in it goes, and comes back new
========================================================================
  deleted checkout-58fb4cf797-65clx (10.244.0.6). the Deployment made checkout-58fb4cf797-d4pp4 (10.244.0.8).
  a new Pod: yes
  restart counts on the replacement (sidecar, checkout): 0 0
========================================================================

Act 6 — injection: a sidecar for a service that never mentions one
========================================================================
  refunds' manifest, k8s/refunds.yaml, sha256 5caac291d7a6, lists these containers: refunds 
  the Pod that is now running lists: sidecar refunds 
  and reads: 2/2
  the manifest the team wrote is unchanged: sha256 5caac291d7a6 (identical)

  The sidecar arrived from outside, beside a service whose own manifest does
  not mention it. This was a patch applied by hand. A service mesh does the same
  thing automatically, for every Pod, through an admission webhook -- which
  this project names and does not build.

Deleting the cluster
```

## What the run shows

- **Act 2** reaches a port that belongs to a different container, with no network settings in the manifest.
  That is a Pod.
- **Act 4** is the correction Tier 1 makes: killing the proxy restarted **only the proxy**. Its restart count went
  from 0 to 1, checkout's stayed 0, and checkout's start time did not change. A crash does not take a neighbour with it.
- **Act 5** is what *is* shared: delete the Pod and both containers go, and the replacement is a new Pod with a new
  address and new containers.
- **Act 6** adds a sidecar to a service whose manifest file has the same hash before and after.

## What this still does not show

Tier 2 narrows the gap between the model and reality. It does not close it.

- **One node.** There is no scheduler choosing between machines, and no node that fails.
- **A control plane that does not fail.** It is one container on your laptop.
- **Injection by hand.** Act 6 applies a patch. A real service mesh does it automatically for every Pod, through a
  mutating admission webhook, which this project names and does not build.
- **No restart back-off.** The proxy was killed once. A container that keeps crashing is restarted with growing
  delays, which this run does not reach.
- **Version skew.** The node image is Kubernetes v1.37.0. A `kubectl` more than one minor version away prints a
  warning and, in this run, still worked. Use one within a minor version of the node.
