# Prerequisites

## Required

- Java threads, and what a lock does.
- What `volatile` means, at a glance: a write by one thread is seen by the others.

## Explicitly not required

- No framework and no database.
- No prior concurrency patterns.

## What you will need

Java 21. `./gradlew run` works offline.
