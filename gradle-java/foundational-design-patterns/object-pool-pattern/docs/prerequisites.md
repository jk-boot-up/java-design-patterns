# Prerequisites

## Required

- Java classes and threads, at the level of `new Thread`.
- What a network connection is, in one sentence.

## Explicitly not required

- No JMH. The benchmark is hand-written, and its method is documented.
- No connection-pool library. The pool is a small class built here.

## What you will need

Java 21. `./gradlew run` works offline.
