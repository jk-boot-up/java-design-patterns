# Prerequisites

## Required

- Data Mapper: this project assumes objects and rows are separate things.
- The difference between `==` and `equals()` in Java.

## Explicitly not required

- No JPA or Hibernate. The mechanism is built by hand.
- No SQL.

## What you will need

Java 21. `./gradlew run` works offline.
