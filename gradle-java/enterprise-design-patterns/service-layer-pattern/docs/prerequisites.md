# Prerequisites

## Required

- Unit of Work: what a transaction is.
- Repository, ideally: the service sits above it.

## Explicitly not required

- No Spring. `@Service` is named as where the pattern has been met.
- No SQL.

## What you will need

Java 21. `./gradlew run` works offline.
