# Prerequisites

## Required

- Data Mapper, Identity Map and Unit of Work, or at least the idea of loading an object graph.
- Java interfaces.

## Explicitly not required

- No Hibernate. The exception is built by hand.
- No SQL.

## What you will need

Java 21. `./gradlew run` works offline.
