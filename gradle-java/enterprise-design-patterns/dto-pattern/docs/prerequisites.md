# Prerequisites

## Required

- Lazy Load, ideally: the order history is a lazy collection.
- Java records.

## Explicitly not required

- No Jackson and no Spring. A small serialiser is built by hand.
- No database.

## What you will need

Java 21. `./gradlew run` works offline.
