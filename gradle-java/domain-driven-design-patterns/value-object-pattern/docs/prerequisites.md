# Prerequisites

## Required

- Java records and immutability, at the level of knowing a record's fields cannot change.
- What `equals` and `hashCode` are for.

## Explicitly not required

- No framework.
- No prior domain-driven design.

## What you will need

Java 21. `./gradlew run` works offline.
